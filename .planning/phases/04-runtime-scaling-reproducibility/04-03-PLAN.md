---
phase: 04-runtime-scaling-reproducibility
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - scripts/compiler.py
  - scripts/pipeline.py
  - scripts/release_validator.py
  - tests/test_compiler.py
  - tests/test_pipeline.py
  - tests/test_release_validator.py
autonomous: true
requirements:
  - RUN-03
user_setup: []
must_haves:
  truths:
    - "RUN-03: Compiler removes the unused `abp_blocking_domains` allocation without changing pruning behavior or output order."
    - "RUN-03: `reports/pipeline-stats.json` exposes inspect-only `runtime_profile` data for large runs."
    - "D-08: `_build_coverage_lookups()` no longer builds the unused `abp_blocking_domains` set."
    - "D-09: Compiler semantic storage remains dictionary/set based; tries, SQLite, partitioned compilation, and external sorting are not introduced."
    - "D-10: Deeper compiler storage redesign is left unimplemented unless Phase 04 metrics later prove it necessary."
    - "D-11: Runtime metrics extend the existing versioned pipeline stats report instead of creating a separate report family."
    - "D-13: Runtime metrics do not create warning thresholds or release hard gates."
    - "D-14: Runtime profile includes durations, byte sizes, worker count, compiler cardinalities, and best-effort memory fields."
  artifacts:
    - path: "scripts/compiler.py"
      provides: "Allocation cleanup and stable compiler cardinality counters"
      contains: "CompileStats"
    - path: "scripts/pipeline.py"
      provides: "Versioned `runtime_profile` report writer data"
      contains: "runtime_profile"
    - path: "scripts/release_validator.py"
      provides: "Pipeline-stats schema compatibility without runtime gates"
      contains: "PIPELINE_STATS_SCHEMA_VERSION"
    - path: "tests/test_compiler.py"
      provides: "No-output-change compiler allocation regression coverage"
    - path: "tests/test_pipeline.py"
      provides: "Runtime profile report schema coverage"
    - path: "tests/test_release_validator.py"
      provides: "Validator accepts runtime profile without warnings/errors"
  key_links:
    - from: "scripts/compiler.py"
      to: "scripts/pipeline.py"
      via: "CompileStats cardinalities projected into runtime_profile"
      pattern: "compiler_cardinalities"
    - from: "scripts/pipeline.py"
      to: "scripts/release_validator.py"
      via: "pipeline-stats schema version"
      pattern: "PIPELINE_STATS_SCHEMA_VERSION"
---

<objective>
Remove obvious compiler allocation waste and add inspect-only runtime-size metrics to the existing pipeline report.

Purpose: Phase 04 needs runtime evidence before deeper compiler storage decisions, while preserving semantic pruning, whitelist safety, and release validation behavior from earlier phases.
Output: `CompileStats` exposes stable cardinalities, `save_stats_json()` writes schema-versioned `runtime_profile`, and `release_validator` accepts the new report schema without adding runtime gates.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-runtime-scaling-reproducibility/04-CONTEXT.md
@.planning/phases/04-runtime-scaling-reproducibility/04-RESEARCH.md
@.planning/phases/04-runtime-scaling-reproducibility/04-VALIDATION.md
@.planning/phases/04-runtime-scaling-reproducibility/04-PATTERNS.md
@.planning/phases/03-release-validation-observability/03-02-SUMMARY.md
@.planning/phases/03-release-validation-observability/03-03-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@scripts/compiler.py
@scripts/pipeline.py
@scripts/release_validator.py
@tests/test_compiler.py
@tests/test_pipeline.py
@tests/test_release_validator.py

<interfaces>
Existing contracts to preserve or extend:
- `@dataclass(slots=True) class CompileStats` is returned by `compile_rules()`.
- `compile_rules(lines: Iterable[str], output_file: str) -> CompileStats` must keep the same signature.
- `save_stats_json(stats: PipelineStats, output_path: str, total_time: float) -> None` is the existing report writer; it can gain an optional runtime-profile parameter if existing tests are updated.
- `PIPELINE_STATS_SCHEMA_VERSION` in `scripts.release_validator` currently equals `1` and is checked by `_validate_report_schema_versions()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove unused compiler lookup allocation safely</name>
  <files>scripts/compiler.py, tests/test_compiler.py</files>
  <read_first>
    - .planning/phases/04-runtime-scaling-reproducibility/04-CONTEXT.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-VALIDATION.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-PATTERNS.md
    - scripts/compiler.py
    - tests/test_compiler.py
    - tests/test_rule_semantics.py
  </read_first>
  <behavior>
    - Removing `abp_blocking_domains` does not alter ABP parent pruning, TLD wildcard pruning, whitelist conflict pruning, duplicate pruning, malformed discard counts, or output ordering.
    - Compiler cardinality metrics are stable inspect-only counts and do not influence pruning decisions.
  </behavior>
  <action>In `tests/test_compiler.py`, add regression coverage for a mixed compile containing ABP parent/child rules, TLD wildcard rules, whitelist exceptions, duplicate semantic rules, and `other_rules`, then assert output lines and pruning counters are unchanged by removing `abp_blocking_domains`. In `scripts/compiler.py`, change `_build_coverage_lookups()` to return only the `tld_wildcards` data actually used, or inline `set(abp_wildcards.keys())` at the call site. Add stable inspect-only `CompileStats` fields for compiler cardinalities such as `abp_rule_keys`, `abp_wildcard_keys`, `exception_rule_keys`, `duplicate_index_size`, and `other_rule_count`; populate them after parsing and before output writing. Do not change `compile_rules()` signature, semantic coverage helpers, whitelist behavior, or storage strategy per D-08 through D-10.</action>
  <verify>
    <automated>python -m pytest tests/test_compiler.py tests/test_rule_semantics.py -x</automated>
    <automated>python -c "from pathlib import Path; import sys; text = Path('scripts/compiler.py').read_text(encoding='utf-8'); sys.exit(1 if 'abp_blocking_domains' in text else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compiler.py` no longer contains `abp_blocking_domains`.
    - `CompileStats` includes compiler cardinality fields populated from existing in-memory structures.
    - Focused compiler and rule-semantics tests pass.
    - No trie, SQLite, partitioned compilation, external sort, or semantic pruning rewrite is introduced.
  </acceptance_criteria>
  <done>Compiler allocation waste is removed and cardinality metrics are available without behavioral change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pipeline stats with inspect-only runtime profile</name>
  <files>scripts/pipeline.py, scripts/release_validator.py, tests/test_pipeline.py, tests/test_release_validator.py</files>
  <read_first>
    - .planning/phases/04-runtime-scaling-reproducibility/04-CONTEXT.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-RESEARCH.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-VALIDATION.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-PATTERNS.md
    - scripts/pipeline.py
    - scripts/compiler.py
    - scripts/release_validator.py
    - tests/test_pipeline.py
    - tests/test_release_validator.py
  </read_first>
  <behavior>
    - `reports/pipeline-stats.json` has `schema_version: 2` and a top-level `runtime_profile` object.
    - `runtime_profile` includes worker count, stage durations, raw input bytes, output bytes, compiler cardinalities, and best-effort memory fields.
    - `scripts.release_validator` accepts schema 2 and does not add warnings or errors for runtime duration, memory, byte size, or cardinality values.
  </behavior>
  <action>In `scripts/pipeline.py`, add a typed runtime profile shape and a compatibility-preserving way for CLI execution to collect it, such as a new `process_files_with_profile(input_dir, output_file)` helper while keeping `process_files(input_dir, output_file) -> PipelineStats` for existing callers. Capture worker count from the cleaning executor configuration, coarse stage durations around clean/compile work, raw input byte total from sorted `*.txt` inputs, output byte size after compile, compiler cardinalities from `CompileStats`, and best-effort memory using stdlib `tracemalloc` and Unix `resource.getrusage()` with graceful `None` values on unsupported platforms. Update `save_stats_json()` to write `schema_version: 2` plus `runtime_profile` per D-11/D-14. In `scripts/release_validator.py`, update `PIPELINE_STATS_SCHEMA_VERSION` and tests so runtime data remains inspect-only per D-13; validator must keep hard gates focused on source health, syntax, canaries, output count, and previous-release deltas.</action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py tests/test_release_validator.py -x</automated>
    <automated>python -m pytest tests/test_compiler.py tests/test_rule_semantics.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `save_stats_json()` writes `schema_version: 2`, existing `statistics`, and top-level `runtime_profile`.
    - Runtime profile includes `worker_count`, `stage_durations_seconds`, `byte_sizes`, `compiler_cardinalities`, and `memory` keys or equivalent documented names.
    - `process_files()` remains callable with the old signature and return type.
    - `release_validator` accepts the new pipeline stats schema without creating runtime warnings or hard failures.
    - No runtime threshold, warning gate, or hard release gate is added.
  </acceptance_criteria>
  <done>Maintainer can inspect runtime-size metrics in the existing pipeline stats report without release gating behavior changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler internals -> pipeline report | Runtime/cardinality observations cross from compiler structures into generated JSON diagnostics. |
| pipeline report -> release validator | Versioned report schema is consumed by validation before publishing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | compiler allocation cleanup | mitigate | Regression tests compare output and pruning counters while removing only the unused lookup allocation per D-08. |
| T-04-03-02 | Repudiation | runtime_profile report | mitigate | Use versioned schema 2 and deterministic field names so runtime evidence is auditable in `reports/pipeline-stats.json` per D-11/D-14. |
| T-04-03-03 | Denial of Service | release validation | mitigate | Keep runtime data inspect-only and update validator tests to prove no runtime warning or hard gate is introduced per D-13. |
</threat_model>

<verification>
Run the focused RUN-03 gates:
- `python -m pytest tests/test_compiler.py tests/test_rule_semantics.py -x`
- `python -m pytest tests/test_pipeline.py tests/test_release_validator.py -x`

Then run the Phase 04 broad gate:
- `python -m pytest tests/test_downloader.py tests/test_pipeline.py tests/test_compiler.py tests/test_release_validator.py tests/test_ci_workflow.py -x`
</verification>

<success_criteria>
- RUN-03 is covered by tests and implementation.
- Compiler allocation cleanup has no output or semantic pruning regression.
- Pipeline stats JSON includes inspect-only runtime profile fields.
- Validator schema compatibility is updated without runtime gates.
- Deep compiler storage redesign remains unimplemented.
</success_criteria>

<output>
Create `.planning/phases/04-runtime-scaling-reproducibility/04-03-SUMMARY.md` when done.
</output>
