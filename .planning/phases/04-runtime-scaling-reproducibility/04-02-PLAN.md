---
phase: 04-runtime-scaling-reproducibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - RUN-02
user_setup: []
must_haves:
  truths:
    - "RUN-02: Pipeline cleaning no longer returns full cleaned list payloads across the process boundary."
    - "D-06: Process-level parallel cleaning remains, but worker results contain bounded spool metadata and stats rather than full `list[str]` payloads."
    - "D-07: Compiler input order remains deterministic according to sorted source-file order, independent of worker completion timing."
    - "D-18: Runtime/scaling behavior uses internal tested defaults and existing CLI surfaces, not a broad runtime config file."
  artifacts:
    - path: "scripts/pipeline.py"
      provides: "Ordered bounded cleaning worker/spool pipeline feeding `compile_rules()` lazily"
      contains: "ProcessPoolExecutor"
    - path: "tests/test_pipeline.py"
      provides: "Regression coverage for deterministic ordered spool consumption and cleanup"
  key_links:
    - from: "scripts/pipeline.py"
      to: "scripts.cleaner.clean_line"
      via: "worker cleaning still delegates single-line decisions"
      pattern: "clean_line"
    - from: "scripts/pipeline.py"
      to: "scripts.compiler.compile_rules"
      via: "ordered generator reads cleaned spools"
      pattern: "compile_rules"
---

<objective>
Reduce pipeline process-boundary materialization while preserving cleaner semantics and deterministic compiler input.

Purpose: Large raw inputs should not force each worker to serialize a full cleaned `list[str]` back to the parent process.
Output: `scripts.pipeline` keeps parallel cleaning but moves cleaned lines through ordered bounded spools or equivalent bounded chunks, with cleanup and deterministic ordering covered by tests.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-runtime-scaling-reproducibility/04-CONTEXT.md
@.planning/phases/04-runtime-scaling-reproducibility/04-RESEARCH.md
@.planning/phases/04-runtime-scaling-reproducibility/04-VALIDATION.md
@.planning/phases/04-runtime-scaling-reproducibility/04-PATTERNS.md
@.planning/phases/03-release-validation-observability/03-02-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@scripts/pipeline.py
@scripts/cleaner.py
@scripts/compiler.py
@tests/test_pipeline.py
@tests/test_integration_pipeline.py

<interfaces>
Existing pipeline contracts to preserve:
- `class PipelineStats(TypedDict)` contains flat counters used by JSON reports and tests.
- `def _clean_single_file(file_path: Path) -> tuple[list[str], dict[str, int]]` is the current materializing boundary to replace.
- `def process_files(input_dir: str, output_file: str) -> PipelineStats` remains callable by tests and CLI code.
- `compile_rules(lines: Iterable[str], output_file: str) -> CompileStats` expects a lazy iterable of cleaned rule lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin ordered bounded cleaning behavior</name>
  <files>tests/test_pipeline.py</files>
  <read_first>
    - .planning/phases/04-runtime-scaling-reproducibility/04-CONTEXT.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-VALIDATION.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-PATTERNS.md
    - scripts/pipeline.py
    - tests/test_pipeline.py
    - tests/test_integration_pipeline.py
  </read_first>
  <behavior>
    - Bounded worker contract: the worker-facing result does not include a full `list[str]` of cleaned lines.
    - Deterministic order: cleaned rules are fed to `compile_rules()` by sorted source-file index even when worker completion order is simulated differently.
    - Cleanup: temporary spool/chunk files are removed after success and after compile failure.
  </behavior>
  <action>Add focused tests in `tests/test_pipeline.py` for RUN-02 and D-06/D-07. Cover a multi-file input where sorted filenames determine compiler input order. Use monkeypatching or temp files to prove the parent consumes worker outputs in source-index order, not completion order. Cover cleanup of temporary spool/chunk files on successful compile and on a `compile_rules()` exception. Keep `clean_line()` as the only cleaning decision function under test; do not duplicate cleaner logic in the pipeline tests.</action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py tests/test_integration_pipeline.py -x</automated>
  </verify>
  <acceptance_criteria>
    - A test fails against the current `_clean_single_file()` full-list return shape or equivalent materialization.
    - Tests assert ordered compiler input for at least three files whose lexical order differs from completion order.
    - Tests assert temporary worker output files are absent after success and after a forced compile failure.
  </acceptance_criteria>
  <done>Pipeline tests define the bounded ordered cleaning contract before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Replace full cleaned-list worker returns with ordered spools</name>
  <files>scripts/pipeline.py, tests/test_pipeline.py</files>
  <read_first>
    - .planning/phases/04-runtime-scaling-reproducibility/04-CONTEXT.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-RESEARCH.md
    - .planning/phases/04-runtime-scaling-reproducibility/04-PATTERNS.md
    - scripts/pipeline.py
    - scripts/cleaner.py
    - scripts/compiler.py
    - tests/test_pipeline.py
  </read_first>
  <action>In `scripts/pipeline.py`, replace `_clean_single_file()` with a bounded worker boundary such as `CleanWorkerResult` plus `_clean_single_file_to_spool()` that writes cleaned lines to a per-input temporary spool file and returns only source index, spool path, and stats. Keep `ProcessPoolExecutor` parallelism per D-06. In `process_files()`, build indexed work from `files = sorted(input_path.glob("*.txt"))`, consume worker results in source-index order, stream spool contents into `compile_rules()`, update `PipelineStats` from returned stats, and remove spools in success and exception paths. Preserve `process_files(input_dir, output_file) -> PipelineStats`, CLI arguments, printed summaries, cleaner discard counters, and compiler stats projection. Do not add public runtime tuning config or a broad config file per D-18.</action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py tests/test_integration_pipeline.py -x</automated>
    <automated>python -c "from pathlib import Path; import re, sys; text = Path('scripts/pipeline.py').read_text(encoding='utf-8'); sys.exit(1 if re.search(r'tuple\[list\[str\]', text) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - No pipeline worker function returns `tuple[list[str], ...]` or a full cleaned list payload.
    - `ProcessPoolExecutor` remains in use for process-level cleaning.
    - Compiler input order still follows `sorted(input_path.glob("*.txt"))`.
    - Temporary spool/chunk files are removed after successful and failed processing.
    - Existing pipeline stats keys and CLI behavior remain compatible.
  </acceptance_criteria>
  <done>Pipeline cleaning avoids unnecessary full-list materialization across workers while keeping deterministic output behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw file -> worker process | Untrusted raw blocklist lines cross into process workers for cleaning. |
| worker spool -> compiler iterator | Cleaned output crosses from temporary worker storage into compiler semantic indexes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | ordered spool merge | mitigate | Preserve sorted source-file index ordering and add tests that completion order cannot alter compiler input per D-07. |
| T-04-02-02 | Denial of Service | worker result serialization | mitigate | Replace full cleaned `list[str]` process returns with bounded spool metadata per D-06. |
| T-04-02-03 | Information Disclosure | temporary cleaned spools | mitigate | Store spools under a temporary runtime directory, avoid source control paths, and clean them on success/failure. |
</threat_model>

<verification>
Run the focused RUN-02 gate:
- `python -m pytest tests/test_pipeline.py tests/test_integration_pipeline.py -x`

Then run the Wave 1 gate after Plan 04-01 also completes:
- `python -m pytest tests/test_downloader.py tests/test_pipeline.py tests/test_compiler.py tests/test_release_validator.py tests/test_ci_workflow.py -x`
</verification>

<success_criteria>
- RUN-02 is covered by tests and implementation.
- Worker results no longer serialize full cleaned `list[str]` payloads.
- Source-file ordering remains deterministic.
- Temporary cleaning artifacts are cleaned up after success and failure.
- No public runtime configuration surface is added.
</success_criteria>

<output>
Create `.planning/phases/04-runtime-scaling-reproducibility/04-02-SUMMARY.md` when done.
</output>
